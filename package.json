{
  "name": "ai-orderops-workbench-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && node deploy/build-sites-dist.mjs",
    "start": "node dist/server/index.js"
  }
}
